import type { ITranslationMessage } from "@/regira_modules/vue/lang"

export default {
    perMonth: {
        en: "per month",
        nl: "per maand",
    },
} as Record<string, ITranslationMessage>
